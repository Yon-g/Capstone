import React from "react";
import { StyledMoveButton } from "./styles";

export default function MoveButton({ type, shape }) {
  return <StyledMoveButton type={type} shape={shape}></StyledMoveButton>;
}
