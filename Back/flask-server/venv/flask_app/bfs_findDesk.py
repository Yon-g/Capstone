from collections import deque

def BFS(MAP):
    mid_x = len(MAP[0]) // 2
    mid_y = len(MAP) // 2

    visited = [[0 for _ in range(MAP[0])] for _ in range(MAP)]
    

    que = deque()
    que.append((mid_x,mid_y))
    while que:
        
    

    return 